const die = "1d3";
let roll = await new Roll(die).evaluate({async: true});

await actor.applyDamage(roll.total * -1);

ChatMessage.create({
  content: `<p>Du hast <b>${roll.total}</b> Lebenspunkte zurückerhalten.</p>`,
  whisper: [game.user.id],
  speaker: ChatMessage.getSpeaker({ actor })
});
